/ Button trigger modal
%button{"data-toggle" => "modal", "data-target" => "#targetModal-#{file.id}", :type => "button"}
  = file.id
/ %button.btn.btn-primary.btn-lg{"data-target" => "#targetModal-#{file.id}", "data-toggle" => "modal", :type => "button"}
/   Launch demo modal
.modal.fade{ "id" => "targetModal-#{file.id}", "aria-hidden" => "true", "aria-labelledby" => "myModalLabel-#{file.id}", :role => "dialog"}
  .modal-dialog{:role => "document"}
    .modal-content
      .modal-header
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
        %h4.modal-title{ id: "#myModalLabel-#{file.id}"} Modal title
