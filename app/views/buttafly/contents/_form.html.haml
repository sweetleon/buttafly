%legend Upload new content
= form_for :originable, url: { controller: "contents"}, :html => {:multipart => true} do |f|
  = f.label :name
  %br/
  = f.text_field :name
  = f.label :flat_file
  = f.file_field :flat_file
  = f.submit "Upload file", class: "button"